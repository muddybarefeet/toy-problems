// Write a function maxDuffelBagValue() that takes an array of cake type objects 
// and a weight capacity, and returns the maximum monetary value the duffel bag can hold.

//all values non negative ints

var cakeTypes = [
    {weight: 7, value: 160},
    {weight: 3, value: 90},
    {weight: 2, value: 15},
];

var capacity = 20;

var maxDuffelBagValue = function (arrOfCakeObjs, weightCap) {

  if (weightCap === 0) return 0;

  //work out max value of cakes from array

  //return value bag can hold
};